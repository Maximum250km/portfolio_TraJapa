
<%= form_with(model: @forum_comment, url:forum_forum_comment_path) do |form| %>
<%= forum.text_area :forum_comments,rows:'5', placeholder:"post your commet"%>
<%= forum.submit "comments" %>
<% end %>

 <p>comments</p>
<h4> <%= forum.forum_comments.count %>　comments</h4>
<thead>
					<tr>
					<th>User</th>
					<th>commnet</th>
					<th>Posted at</th>
					<th></th>
				</tr>
				</thead>
				<tr>
				<% forum.forum_comments.each do |c| %>
				<td><%= c.account_name %></td>
				<td><%= c.forum_comments %></td>
				<td><%= c.created_at.strftime('%Y/%m/%d') %></td>
			</tr>
				<% if c.user == current_user %>
				<td><%= link_to "削除",  forum_forum_comment_path(c.forum_id, c.id), method: :delete, remote: true %></td>
					</tr>
				<% end %>
				<% end %>
			</table>



<td id="forum_comments"></td>
	<p>comments</p>
	<h4><%= @forums.forum_comments.count %>　comments</h4>
	<thead>
		<tr>
			<th>User</th>
			<th>commnet</th>
			<th>Posted at</th>
			<th></th>
		</tr>
	</thead>
	<tr>
		<% @forums.forum_comments.each do |c| %>
			<td><%= c.account_name %></td>
			<td><%= c.forum_comments %></td>
			<td><%= c.created_at.strftime('%Y/%m/%d') %></td>
		<% end %>
	</tr>
	<% if c.user == current_user %>
		<td><%= link_to "削除",  forum_forum_comment_path(c.forum_id, c.id), method: :delete, remote: true %></td>
	<% end %>
</div>




<%= form_with(model: @forum_comment, url:forum_forum_comment_path) do |form| %>

	<%= form.text_area :forum_comments %>
	<%= form.submit "comments" %>
<% end %>
<% end %>